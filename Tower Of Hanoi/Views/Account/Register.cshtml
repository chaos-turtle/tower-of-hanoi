@model Tower_Of_Hanoi.Models.RegisterViewModel
@{
    ViewBag.Title = "Register";
}

<main aria-labelledby="title">
    <h2 id="title">@ViewBag.Title</h2>

    @using (Html.BeginForm("Register", "Account", FormMethod.Post, new { role = "form" }))
    {
        @Html.AntiForgeryToken()
        <h4>Create a new account</h4>
        <hr />
        @Html.ValidationSummary("", new { @class = "text-danger" })
        <div class="row">
            <div class="col-md-12">
                @Html.LabelFor(m => m.Email, new { @class = "col-md-2 col-form-label" })
                @Html.TextBoxFor(m => m.Email, new { @class = "form-inline" })
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                @Html.LabelFor(m => m.Password, new { @class = "col-md-2 col-form-label" })
                @Html.PasswordFor(m => m.Password, new { @class = "form-inline" })
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                @Html.LabelFor(m => m.ConfirmPassword, new { @class = "col-md-2 col-form-label" })
                @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-inline" })
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <input type="submit" value="Register" class="btn btn-outline-dark" />
            </div>
        </div>
    }
</main>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
